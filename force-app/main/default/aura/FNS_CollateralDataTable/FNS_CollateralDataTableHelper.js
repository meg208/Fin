({
	initData : function(component, event, helper) {
		 /*component.set('v.columnNames', [
        {label: 'COLLATERAL FILE', fieldName: 'FNS_Application__c', type: 'text', typeAttributes: { target: '_self'}},
             {label: 'COLLATERAL TYPE', fieldName: 'FNS_CollateralType__c', type: 'text'},
            {label: 'PRODUCT RELATED', fieldName: 'FNS_Application__c', type: 'url', typeAttributes: { target: '_self'}},
           {label: 'PRIMARY COLLATERAL', fieldName: 'FNS_CollateralCategory__c', type: 'text'}
        ]);
        
         var action = component.get("c.fetchCollateralData");
         action.setParams({
        });
         action.setCallback(this, function(response){
             var state = response.getState();
             component.set('v.isLoading',false);
             if (state === "SUCCESS") {
                component.set("v.rowData", response.getReturnValue());
                console.log(component.get("v.rowData"));
             }
        });
         $A.enqueueAction(action);*/
	}
})